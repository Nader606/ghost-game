{
  "name": "ghost-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit",
    "build": "npm run lint && npm run typecheck && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "p5": "^1.11.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "@types/p5": "^1.7.6",
    "typescript": "^5.6.0",
    "vite": "^8.0.0"
  }
}
